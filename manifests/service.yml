apiVersion: v1
kind: Service
metadata:
    name: sreelathadeebaguntlaazurecloud
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sreelathadeebaguntlaazurecloud